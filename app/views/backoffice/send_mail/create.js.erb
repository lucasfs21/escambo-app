$(function(){
  let $sendMailModal = $('#sendMailModal');
  // Shows Modal
  $sendMailModal.modal('hide');
  $('#subject-text').val('');
  $('#message-text').val('');
  $.notify( {message: '<%= @notify_message %>'}, { type: '<%= @notify_flag %>', placement: { from: 'top', align: 'center'}});
})
